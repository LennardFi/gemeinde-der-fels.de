import Window from "@/components/surfaces/window/Window"
import WindowHeader from "@/components/surfaces/window/WindowHeader"

export default function Loading() {
    return (
        <Window>
            <WindowHeader title="Ansicht wird geladen..." />
        </Window>
    )
}
